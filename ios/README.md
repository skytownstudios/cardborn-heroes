# iOS

SwiftUI shell with Gilded Ivory theme and 5-tab layout. Open in Xcode to create the Xcode project and wire shared content sync.

Gameplay parity with Android is planned per `docs/REBUILD_PLAN.md`.
